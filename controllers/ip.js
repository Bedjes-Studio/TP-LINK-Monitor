// TODO : Read all ips and send them to client
exports.read = (req, res, next) => {
    res.status(500).json({ error: "TODO" })
};

// TODO : Add ip to the whitelist
exports.whitelist = (req, res, next) => {
    res.status(500).json({ error: "TODO" })
};

// TODO : Remove ip to the whitelist
exports.unWhitelist = (req, res, next) => {
    res.status(500).json({ error: "TODO" })
};